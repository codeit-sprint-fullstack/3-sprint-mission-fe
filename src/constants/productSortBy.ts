const PRODUCT_SORT_BY = {
  recent: Object.freeze({
    string: "날짜순",
    parameter: "recent",
  }),
  favorite: Object.freeze({
    string: "좋아요순",
    parameter: "favorite",
  }),
};

export default PRODUCT_SORT_BY;
